seed: 20260716
condition_source: rgt
conditioning: multiscale_film_rgt_only
output_root: outputs/rgt
latent_cache_root: data/rgt_latents
latent_stats_path: checkpoints/rgt_latent_stats.pt
vae_checkpoint: checkpoints/geovae.ckpt
valid_sx_root: data/rgt/valid/seismic
valid_rgt_root: data/rgt/valid/rgt
raw_crop_txy:
- 256
- 256
- 256
batch_size: 16
num_workers: 6
vae_tiled: false
epochs: 90
lr: 5.0e-05
warmup_steps: 75
weight_decay: 0.0001
grad_clip: 0.5
width: 192
res_blocks: 4
dropout: 0.0
timesteps: 1000
ema_decay: 0.995
prediction_type: v
sample_steps: 50
sample_x0_clip: 5.0
samples_per_validation: 4
sample_every_epochs: 5
latent_scale: 5.0
z_clip: 3.2
log_every_steps: 5
train_sx_root: data/rgt/train/seismic
train_rgt_root: data/rgt/train/rgt
cache_batch_size: 1
expected_train_crops: 3800
expected_valid_crops: 50
cache_train_views: 4
cache_valid_views: 1
